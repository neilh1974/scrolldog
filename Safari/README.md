# Safari Project Output

This folder is reserved for the generated Safari app project.

Run the helper from the project root:

```sh
tools/convert_safari.sh
```

The helper needs Xcode. This Mac currently has Apple command line tools only, so conversion cannot run here until Xcode is installed and selected.
